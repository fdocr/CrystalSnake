class Strategy::Utils
  # This class bundles a bunch of utility methods that can be used by 
  # strategies. Each method will always be an instance method and is 
  # implemented in an unique file in the `/strategies/utils/` directory.
  # Example call: `Strategy::Utils.a_star(a, b)`
end